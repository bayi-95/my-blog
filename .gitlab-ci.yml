image: node:18
pages:
  stage: build
  tags:
    - shell
    - window
  cache:
    paths:
      - node_modules/
  script:
    - pnpm install
    - pnpm build
  artifacts:
    expire_in: 2 week
    paths:
      - public
  only:
    - master
